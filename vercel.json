{
  "buildCommand": "npm install && npx prisma generate --schema=packages/db/schema.prisma && cd apps/web && npm run build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "echo 'install handled in buildCommand'",
  "framework": "nextjs",
  "regions": ["fra1"]
}
